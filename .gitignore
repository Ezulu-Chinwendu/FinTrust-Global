# Terraform state and local cache
.terraform/
*.tfstate
*.tfstate.backup
.terraform.lock.hcl

# Lambda build artifacts — rebuild locally, don't commit binaries
lambda/function.zip
lambda/node_modules/

# AWS credentials — NEVER commit these
.env
*.pem

# macOS
.DS_Store

